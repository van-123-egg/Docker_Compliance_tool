global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'docker-compliance-scanner'
    static_configs:
      - targets: ['host.docker.internal:8000']
        labels:
          instance: 'compliance-tool'
